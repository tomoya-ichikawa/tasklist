<h1>タスク一覧</h1>

<ul>
    <% @tasks.each do |task| %>
    <li>
        <%= link_to task.id, task_path(task.id) %>
        <%= task.status %>
        &gt;
        <%= task.content %>
    </li>
    <% end %>
</ul>
<%= paginate @tasks %>

<p>
    <%= link_to 'タスクの作成' ,new_task_path %>
</p>

