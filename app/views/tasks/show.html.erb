<h1>id:
<%= @task.id %>のタスク詳細ページ</h1>

<P>
    <%= @task.content %>
</P>

<p><%= link_to 'タスクの編集', edit_task_path(@task.id) %>
</p>

<p>
    <%= link_to 'このタスクを削除する', task_path(@task.id), method: :delete,data: {confirm: '本当に削除してよろしいですか'} %>
</p>

<%= link_to '一覧に戻る', tasks_path %>